import React, { memo } from "react";
import { Handle, Position } from "@xyflow/react";
import { Card } from "@/ui";
import BaseNode from "../base-node/node";
/**@todo any类型 */
export default memo((props: any) => {
  return <BaseNode nodeProps={props}>hahah</BaseNode>;
});
